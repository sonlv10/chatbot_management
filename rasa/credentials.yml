# Rasa Credentials Configuration
# This file defines the credentials for external services

# REST channel for API access
rest:

# Socket.io channel for real-time chat
socketio:
  user_message_evt: user_uttered
  bot_message_evt: bot_uttered
  session_persistence: true
